/*
 * 独自 JavaScript。本体 default_frame.twig が
 *   <script src="{{ asset('assets/js/customize.js', 'user_data') }}"></script>
 * で自動読込します（ページ末尾・function.js / eccube.js の後）。
 * jQuery は本体が読み込み済みなのでそのまま使えます。
 */
document.addEventListener('DOMContentLoaded', function () {
  // ここに独自スクリプトを追記してください。
});
